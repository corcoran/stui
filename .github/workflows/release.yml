name: Release

on:
  push:
    tags:
      - 'v*.*.*'

env:
  CARGO_TERM_COLOR: always

jobs:
  build-linux-x86:
    name: Build Linux x86_64
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-musl

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          key: linux-x86_64-musl
          shared-key: source-deps
          cache-on-failure: true

      - name: Install musl tools
        run: sudo apt-get update && sudo apt-get install -y musl-tools

      - name: Build release binary
        run: cargo build --release --target x86_64-unknown-linux-musl

      - name: Create archive
        run: |
          mkdir -p package/stui
          cp target/x86_64-unknown-linux-musl/release/stui package/stui/
          cp README.md CHANGELOG.md config.yaml.example package/stui/
          cd package
          tar -czf ../stui-linux-x86_64.tar.gz stui

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: stui-linux-x86_64
          path: stui-linux-x86_64.tar.gz

  build-linux-arm:
    name: Build Linux ARM64
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-unknown-linux-musl

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          key: linux-aarch64-musl
          shared-key: source-deps
          cache-on-failure: true

      - name: Install cross
        run: cargo install cross --locked

      - name: Build release binary
        run: |
          export HOSTNAME=$(hostname)
          cross build --release --target aarch64-unknown-linux-musl

      - name: Create archive
        run: |
          mkdir -p package/stui
          cp target/aarch64-unknown-linux-musl/release/stui package/stui/
          cp README.md CHANGELOG.md config.yaml.example package/stui/
          cd package
          tar -czf ../stui-linux-aarch64.tar.gz stui

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: stui-linux-aarch64
          path: stui-linux-aarch64.tar.gz

  build-macos-intel:
    name: Build macOS Intel
    runs-on: macos-13
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          key: macos-intel
          shared-key: source-deps
          cache-on-failure: true

      - name: Build release binary
        run: cargo build --release

      - name: Create archive
        run: |
          mkdir -p package/stui
          cp target/release/stui package/stui/
          cp README.md CHANGELOG.md config.yaml.example package/stui/
          cd package
          tar -czf ../stui-macos-intel.tar.gz stui

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: stui-macos-intel
          path: stui-macos-intel.tar.gz

  build-macos-arm:
    name: Build macOS Apple Silicon
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          key: macos-arm64
          shared-key: source-deps
          cache-on-failure: true

      - name: Build release binary
        run: cargo build --release

      - name: Create archive
        run: |
          mkdir -p package/stui
          cp target/release/stui package/stui/
          cp README.md CHANGELOG.md config.yaml.example package/stui/
          cd package
          tar -czf ../stui-macos-arm64.tar.gz stui

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: stui-macos-arm64
          path: stui-macos-arm64.tar.gz

  build-windows:
    name: Build Windows x86_64
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          key: windows-x86_64
          shared-key: source-deps
          cache-on-failure: true

      - name: Build release binary
        run: cargo build --release

      - name: Create archive
        shell: pwsh
        run: |
          mkdir -p package/stui
          cp target/release/stui.exe package/stui/
          cp README.md package/stui/
          cp CHANGELOG.md package/stui/
          cp config.yaml.example package/stui/
          cd package
          Compress-Archive -Path stui -DestinationPath ../stui-windows-x86_64.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: stui-windows-x86_64
          path: stui-windows-x86_64.zip

  create-release:
    name: Create GitHub Release
    needs: [build-linux-x86, build-linux-arm, build-macos-intel, build-macos-arm, build-windows]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create release
        uses: softprops/action-gh-release@v1
        with:
          draft: true
          files: |
            artifacts/stui-linux-x86_64/stui-linux-x86_64.tar.gz
            artifacts/stui-linux-aarch64/stui-linux-aarch64.tar.gz
            artifacts/stui-macos-intel/stui-macos-intel.tar.gz
            artifacts/stui-macos-arm64/stui-macos-arm64.tar.gz
            artifacts/stui-windows-x86_64/stui-windows-x86_64.zip
          body: |
            ## Installation

            Download the appropriate archive for your platform and extract it:

            **Linux (x86_64):**
            ```bash
            tar -xzf stui-linux-x86_64.tar.gz
            sudo cp stui/stui /usr/local/bin/
            ```

            **Linux (ARM64):**
            ```bash
            tar -xzf stui-linux-aarch64.tar.gz
            sudo cp stui/stui /usr/local/bin/
            ```

            **macOS (Intel):**
            ```bash
            tar -xzf stui-macos-intel.tar.gz
            sudo cp stui/stui /usr/local/bin/
            ```

            **macOS (Apple Silicon):**
            ```bash
            tar -xzf stui-macos-arm64.tar.gz
            sudo cp stui/stui /usr/local/bin/
            ```

            **Windows:**
            Extract the zip file and add the `stui` folder to your PATH, or copy `stui/stui.exe` to a directory in your PATH.

            ## Configuration

            See the included `config.yaml.example` file and [README.md](https://github.com/${{ github.repository }}/blob/${{ github.ref_name }}/README.md#configuration) for setup instructions.
